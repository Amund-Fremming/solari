# Tasks

- [ ] builder pattern from env vars for all different type of payment services
- [ ] trait for pay so the top level service just gets the type then `.pay()`
- [ ] test app to use it as a api
- [ ] test app react native to test to pay
- [ ] good responses to show how much was paid, the status of some sort and how much was paid
